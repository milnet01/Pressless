# PRESS-0013 — review loop log

`review-contract` rows for `docs/specs/PRESS-0013-publish.md`.

## Cold-eyes loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-09-17 | 3, cold — genre pinned `spec`; a new spec, whole document gated. Packet carried design rules 1, 9 and 10 and § Errors, windows on PRESS-0008, 0009, 0011, 0012, 0021 and 0022, and the Publisher, Credentials, Settings, Store, Builder, editor, Face, setup and launch code. GitHub, a browser and Windows declared unrunnable | 0 | 5 | 4 | 1 | **Ten verified, ten fixed, none dismissed.** Nothing let a caller tell an unforeseen upload failure from a build failure; the guard refused the deletes and undo design rule 9 exempts; a failed bin after a good publish read as a failure; a put-back could not be byte-identical through the Store; `save` and its caller could both take the lock; an ordinary draft published its `Replaces`; a capture could hold its lock through the upload; the failure reply pinned `draft` true; the re-fixtured launch test opened a browser; INV-1 named the wrong request. |
| 2 | 2026-09-17 | 3, cold — identical brief, packet rebuilt from disk with a window on `face.capture` added, no prior-loop findings carried | 1 | 3 | 3 | 0 | **Seven verified, seven fixed, none dismissed. Cap reached (2 for a spec); the tail is empty and the spec routes to implementation.** Notices from the move had no way back; the build's place inside the capture was unstated; a kept copy made the next save fail; the lock across the upload stalled the editor pages; the published date had two sources; `_now` named no module; decision 9 claimed the release job tests serving. A violent cap: four of seven landed on loop 1's text, so this document is not gated again as it stands. All seventeen verified findings sit in the gated span, the whole new document. |
| 3 | 2026-09-26 | 3, cold — genre pinned `spec`; re-gate for PRESS-0147 (armed at a34d738). All lanes held every question. Lanes disclosed the arming commit's subject from the git snapshot | 0 | 0 | 0 | 1 | **One verified, one fixed, none dismissed.** INV-4's new clause named the failure sentence where it meant a notice. Three open questions, none became a finding. |
| 4 | 2026-09-26 | 3, cold — identical brief, packet rebuilt from disk, no prior-loop findings carried. All lanes held every question. Lanes disclosed loop 3's commit subject from the git snapshot | 0 | 0 | 0 | 0 | **Converged: no finding.** Open questions: § 6 has no row for an unknown outcome with a copy left; § 4.3 and INV-4 settle it, so not material. |
