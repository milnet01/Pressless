# PRESS-0014 — review loop log

`review-contract` rows for `docs/specs/PRESS-0014-fixed-pages.md`.

## Cold-eyes loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-09-25 | 3, cold — genre pinned `spec`; first gate on a new document. Each lane held all four questions. Packet: builder, store, editor, publishing and face windows, and the cited sections of PRESS-0006, 0008, 0012, 0013 and 0015. The browser, GitHub and Windows declared unrunnable. The packet stated 531 lines against 532 on disk | 1 | 1 | 3 | 2 | **Seven verified, seven fixed, none dismissed.** All three lanes found the marker claim false: the Builder closes a block at the next END of the same kind, not at any END. The Builder now exports `furniture_spans`. All three found no field saying which file a window read, which INV-7 needs; `waiting` is added. Two found `pieces` ambiguous about what it returns, and two found no reply shape for a refusal during Publish. One found the newest-entry preview could pick an entry `build` filters out. Two open questions became Q4s: INV-9's key refusal could fall before the move, and INV-12's words case could not catch its mutation. |
| 2 | 2026-09-25 | 3, cold — identical brief; the spec copy and its line count refreshed, the cited sources unchanged since loop 1 (checked with `git diff`). Each lane held all four questions | 0 | 3 | 2 | 0 | **Five verified, five fixed, none dismissed. Cap reached (2 for a spec); the tail is empty.** All three lanes found that `furniture_spans` raising on an unpaired marker broke the words view and the stray-text notice on the very file § 6 says saves; such a file now opens in the code view. One lane each: no owner for the list's new section, so `editor._list` draws it; `register` took no `transport` for INV-9; two line-ending rules disagreed on the last line; the reply sample hard-coded `waiting` true. **A calm cap**: two of five landed on loop 1's text, each a loop-1 decision not yet propagated, not a repair of a repair. The document is new, so no second share. One open question resolved clean: the home page's tile images sit in `assets/`, which previews serve. |
