# PRESS-0003 — The rolling log: review loop log

The rows for `docs/specs/PRESS-0003-log.md`, kept here per
`spec-format.md` § 6. `review-contract` writes them, one per loop as it
closes; a landed row is never edited.

## Cold-eyes loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-09-08 | 3, cold — genre pinned `spec`; first gate on a new document for a module that does not exist. Packet carried `docs/design.md` § Logging whole (gated the same day), § Where everything sits on disk, `security.md` § 6, the `settings.py` surface, both cited existing tests, and the rolling and failure measurements executed in session. Windows / PyInstaller / GitHub / Analytics declared unrunnable | 4 | 1 | 2 | 1 | **Eight verified, eight fixed, none dismissed; three candidates kept.** **All three lanes found the same two defects.** The first was a false claim, and it was the spec's central one: § 2 said the design puts the obligation on the raise site *"so by the time text reaches this module it is already clean"* — which does not follow, because a failure no part of Pressless raised has no raise site, and a stock file error quotes the path it failed on. The design already assigns that class to the Face, type only; the spec left it assigned to nobody at the one place it bites, the `note()` call. An implementer of PRESS-0011 reading this spec as the log's identifying contract would have written `note(f"failed: {exc}")` and put a home directory in the file. The clause is deleted and both obligations are named. The second was smaller and mechanical: § 7 said *two* tests need an unwritable directory when only one does, and its skip clause then reached the missing-folder test, which runs anywhere — so INV-3's other half would have skipped on Windows, the platform this project must ship to. **One lane found INV-3 unfalsifiable for a third of its own surface**: `close` is the entry point that flushes, so it is where a full disk raises, and no test touched it; a `close` written outside its `try` would have kept every test green. **One lane found § 7's *one test per invariant* false** against § 5's seven for five, which would have cost two tests. **Two lanes found the encoding unpinned, and executing it made it worse than filed**: with a narrow encoding a line carrying one character outside it does not appear in the file at all while its neighbours do, and `logging` prints its own traceback to stderr — a traceback quotes absolute paths, so the default is a leak route as well as a data-loss route. UTF-8 is pinned, and the handler's `handleError` is silenced rather than the process-wide `raiseExceptions` flag. **Two were the orchestrator's own, caught by the fix-time checks rather than by a lane.** § 7 gained *"every test writes to a real temporary directory rather than a double"* three paragraphs before requiring a double for the flush test. And the 4b sweep found the encoding was decided fresh when `settings.py`, `credentials.py`, `store.py` and `insights.py` all already pin UTF-8 and PRESS-0006 INV-10 states it — so the spec now conforms to precedent rather than inventing, and the line-ending departure it implies is stated: `FileHandler` accepts no `newline` (verified: `TypeError`), so the log alone takes the platform's, which is right for a file a person reads and nothing parses. **Candidates kept, not fixed:** `spec_lint` reports `test_coverage_gap` for INV-2, INV-4 and INV-5 because § 7 names INV-1 and INV-3 as the exceptions to the test count and the check reads that as an exhaustive list — its own message documents the limitation (ANTS-4890), and the fix would be a third copy of a list § 5 and § 10 already carry. |
| 2 | 2026-09-08 | 3, cold — identical brief; packet rebuilt whole from disk, re-measured, and extended with the interpreter's own handler signatures and the encoding measurement, all facts about unchanged stdlib source | 2 | 0 | 0 | 2 | **Four verified, four fixed, none dismissed; the same three candidates kept. Cap reached (2 for a spec); the tail carries no unfixed finding and the run exits.** **A CALM cap: two of the four landed on text this run wrote**, checked against loop 1's ledger rather than recall. On a new document the run's other measure does not discriminate — the whole spec is the change being gated, so every finding falls inside it by construction. **Both Q1s were claims that reading could not settle and executing did.** Two lanes found *"`ENCODING` is UTF-8, which holds every character Python can put in a `str`"*; executed, a lone surrogate raises `UnicodeEncodeError`, and one lane placed it exactly — an undecodable filename returns through `surrogateescape` carrying surrogates, and § 2 names a stock file error quoting its path as the one message class arriving unstripped. So the class the spec expects raw is the class most likely to break the encoding it had just pinned, losing the line in the silence § 4.3 exists to prevent. `errors="backslashreplace"` is now pinned beside it. **Two lanes found the failed-roll row backwards, and one named its own uncertainty for want of a shell.** Executed: `emit` rolls before it writes, so a failing roll drops the line rather than appending it, and having closed the stream it fails identically on every later call — twelve lines through such a handler left three written and the file frozen *under* its limit. The spec said the file grows unbounded; the truth is the opposite and worse, the record stopping silently while the bound holds. § 9 now states that and takes the accept-rather-than-handle decision against the real behaviour instead of the imagined one. **One lane found the leak rule had no invariant**: § 4.4 silences `handleError` because the default prints a path-bearing traceback, and INV-3's tests assert only that no exception *escapes* — which the default satisfies, because it prints rather than raises. Every test would have stayed green with the override absent, which is what an implementer gets by doing nothing. INV-6. **The sharpest finding was one lane's and it subsumed another's:** `test_note_survives_an_unwritable_folder` could not reach `note`'s failure path at all, because § 4.4 hands back a Log that writes nothing — so it exercised the null Log, duplicated the missing-folder test, and would have shipped `note` unguarded with a green suite. A second lane reached the same test by another road: `chmod` does not bind root, so in a container or the pre-push hook's isolated checkout it passes vacuously. INV-3's tests are now one per *reachable failure* rather than one per entry point, and the surviving fixture skips on the observed state rather than on the platform. **Route from here: implementation**, which is what the cap of 2 is set for. |
