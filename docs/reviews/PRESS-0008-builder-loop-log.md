# PRESS-0008 — review loop log

`review-contract` rows for `docs/specs/PRESS-0008-builder.md`.

## Cold-eyes loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-09-13 | 3, cold — genre pinned `spec`; a new spec, whole document. Packet carried the design's rules and its disk, logging and photograph passages, windows on PRESS-0001, 0004, 0005, 0006, 0007, 0009 and 0011, and the Store, Marks, Settings, Face and Publisher code. Today's generator and live site, Windows and GitHub unrunnable | 1 | 4 | 11 | 5 | **Twenty-one findings: nineteen fixed, one surfaced, one dismissed. Loop 1 of up to two.** Two or more lanes: the excerpt had no source, label and stray-file differences would fail the live-site test, and the reproducibility test could not vary hash seed or file order. One lane each: format decided by extension, XMP untested, a third crash point, a foreign folder replaced, the write-failure type, a trailing slash, and three tests that could not fail. Surfaced: a preview has no local stylesheet (S10). Dismissed: the design's dated count. |
| 2 | 2026-09-13 | 3, cold — identical brief; packet rebuilt whole from disk and extended with the Store's category guard and `read_html` | 0 | 4 | 4 | 4 | **Twelve findings: eleven fixed, one dismissed. Cap reached (2 for a spec); the tail is empty.** A calm cap: five of the eleven landed on text loop 1 wrote. Every finding of the run falls inside the gated span, a new document. Two lanes: the sitemap's entry address. One lane each: Import's furniture source, `MPO`, a comment-block fixture, a GIF publishing its original, the preview change the design hands over, the rollback on a failed rename, and three tests that could not fail. Dismissed: a build-time limit with nothing measured to set it. |
| 3 | 2026-09-17 | 3, cold — amendment gate, one loop by the user's budget for an amendment; packet windowed Marks' structure, captions and parse, and the built Builder's text functions | 0 | 0 | 0 | 1 | **One verified, one fixed. Not converged: one loop is the budget.** All three lanes: INV-16's third case could not see captions added to words, since an untitled label is cut short. Share on text this run wrote: 0 of 1; inside the gated span: 1 of 1. Open questions, none a finding: whether whitespace is judged before or after references are decoded (the implementer's call), and whether INV-15's heading means a card label (it is the page's). |
