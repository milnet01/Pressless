# PRESS-0021 — review loop log

`review-contract` rows for `docs/specs/PRESS-0021-setup.md`.

## Cold-eyes loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-09-17 | 3, cold — genre pinned `spec`; packet carried Settings, Credentials, Publisher, Builder, Store and Face windows and the cited spec passages, plus the executed malformed-key results | 2 | 3 | 1 | 4 | **Ten verified, ten fixed, none dismissed.** All three lanes: two Face sentences still sent him to a site-folder setting the page does not have. Q2s: a site-folder refusal is a file from another machine, which PRESS-0001 sends to setup and this spec had left with no form; `GITHUB_ACCOUNT` was wrongly listed as an upgrade-breaking name. Q4s: every test needed a published entry or `NoWriting` answered first; the file store refuses on Windows CI, so credentials are doubled; INV-14 had no positive control; INV-13's `{secret}` half could not fail. Q3: the form's field names. Q1s: the `NoStore` sentence does not say setup stopped; a save failure left no file "as it is". Four open questions resolved clean. |
