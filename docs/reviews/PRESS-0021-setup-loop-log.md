# PRESS-0021 — review loop log

`review-contract` rows for `docs/specs/PRESS-0021-setup.md`.

## Cold-eyes loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-09-17 | 3, cold — genre pinned `spec`; packet carried Settings, Credentials, Publisher, Builder, Store and Face windows and the cited spec passages, plus the executed malformed-key results | 2 | 3 | 1 | 4 | **Ten verified, ten fixed, none dismissed.** All three lanes: two Face sentences still sent him to a site-folder setting the page does not have. Q2s: a site-folder refusal is a file from another machine, which PRESS-0001 sends to setup and this spec had left with no form; `GITHUB_ACCOUNT` was wrongly listed as an upgrade-breaking name. Q4s: every test needed a published entry or `NoWriting` answered first; the file store refuses on Windows CI, so credentials are doubled; INV-14 had no positive control; INV-13's `{secret}` half could not fail. Q3: the form's field names. Q1s: the `NoStore` sentence does not say setup stopped; a save failure left no file "as it is". Four open questions resolved clean. |
| 2 | 2026-09-17 | 3, cold — identical brief; packet rebuilt whole from disk and widened with `save`'s error handling, the Face's capture lock, `builder.build` and PRESS-0009 § 4.1 | 2 | 2 | 0 | 3 | **Seven verified, seven fixed, none dismissed. Cap reached (2 for a spec); tail empty; ships to implementation.** Two lanes: a mistyped repository reaches setup as `RemoteStateMissing`, not `RepositoryMissing`, so it is now a refused `repository` answer — executed through `publisher._failure`. All three: the never-does list contradicted loop 1's carried-file first run. Q4s: INV-4's version-2 fixture is refused by `save` itself, so it could not show an overwrite (replaced by one `save` accepts, executed); INV-2's Settings half could pass; INV-10's two runs needed separate folders. Q1: "open Settings and save" fixes almost none of the site-folder causes, so it is gone. **Violent cap:** three of seven anchors sit in loop 1's text, and two more are contradictions a loop-1 fix created. Not re-gated as it stands. All seventeen findings of the run fall inside the gated span, which is the whole new document. Three open questions resolved clean. |
