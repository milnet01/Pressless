# PRESS-0019 — Insights: review loop log

The rows for `docs/specs/PRESS-0019-insights.md`, kept here per
`spec-format.md` § 6. `review-contract` writes them, one per loop as it
closes; a landed row is never edited.

## Cold-eyes loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-09-06 | 3, cold — genre pinned `spec`; first gate on a new document. Packet carried `insights.py` whole, the test header this spec replaces, every test name, and PRESS-0020's bullet quoted whole. The module ships, so Q1 was fully in scope | 5 | 1 | 4 | 1 | **Nine verified, nine fixed.** **All three lanes found the same two defects, and both were mine.** The first: §4.4, §4.5 and §6 restricted the stale-cache fallback to Google being unreachable, and `read()` catches the base failure type — executed, a 401, a 429 and a 500 each return a cached reply marked stale rather than raising. An implementer narrowing the handler to match the document would take the dashboard down on an expired token, which is the outcome §4.4 says the design rejects. The second: INV-18's behaviour already ships, so calling it work still to do made §7 demand a red run nobody can produce without building something broken on purpose. **All three lanes disputed the packet fact that said otherwise**, which is the brief's own instruction working. **Two lanes found the spec citing PRESS-0020 for a window set that bullet does not name**, leaving the cache's only bound recorded in no artefact but this one, and two found `_store` citing INV-8 for a promise this document gives INV-24. **Singles:** `max_age_seconds` is the quota guard and the caller picks it, with neither default's value stated; the base URL and API version were pinned nowhere; INV-6 dropped the `RESERVED_` literal the header it replaces had pinned, and INV-8 dropped the cache file's name. **One was mine, caught while executing a fix**: the new prefix clause justified itself with a hazard that cannot occur, a two-letter country code being unable to contain the marker. **Also fixed before dispatch, from resolving citations rather than recalling them:** every one of the sixteen carried `*Test:*` names was invented and wrong, and `docs/design.md` § State keeps the last reply singular, which the cache change falsifies — recorded in §11 for its own gate rather than edited here. |
| 2 | 2026-09-06 | 3, cold — identical brief, packet rebuilt from disk with loop 1's corrected facts; the module ships, so Q1 stayed fully in scope | 3 | 3 | 1 | 0 | **Six verified, six fixed. Cap reached (2 for a spec); the tail is empty and the document routes to implementation. A VIOLENT cap: five of the six landed on text this run wrote**, each anchor checked against loop 1's ledger rather than recall — so a third cold read would mostly be repairing the second, and the cache change is the better next reviewer. On a new document the run's other measure does not discriminate: the whole spec is the change being gated, so every finding falls inside it by construction. **All three lanes found the same defect, and it was loop 1's own repair over-reaching**: narrowing §4.5 to the no-cache path, I wrote that a cached reply suppresses every row of that table, when the property-id and site-folder refusals both run before the cache is read. A conformer would have wrapped them in the fallback — serving stale numbers to a writer who never configured Analytics, and accepting a cache folder inside the published site folder whenever a reply happened to be cached there, which is the leak INV-23 exists to stop. **One lane found §7's *every test hands in a double* false**, a sentence carried verbatim from the header this spec replaces: it was true of sixteen invariants and stopped being true when INV-19 to INV-21 arrived, since those exercise the module's own client, which the seam replaces. **One found INV-24 covering the sync alone** while §11 tells an implementer to repoint `_store`'s citations at it — the no-temporary-left-behind half would have been dropped on the way. **One found the base URL given where the request target was meant**, so the address as written 404s. **One found the tie-break unpinned**: only the fetch path sorts, so without ties-by-code a cached reply and a fresh one list identical data in different orders. **One found §1 claiming every citation still resolves** while §11 says two of them move. **Loop 1's own count of what shipped was wrong in the packet too** — all three lanes disputed it there and were right, which is the brief working as designed. |
