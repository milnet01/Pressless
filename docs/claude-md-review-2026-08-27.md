# CLAUDE.md — review loop log

`CLAUDE.md` is read in full by every session on every turn, so its review
history is kept here rather than in the file. `review-contract` is the only
writer of these rows, and a row is written as its loop happens — back-filling
destroys the audit trail the log is kept for. A row that has landed is never
edited; a correction goes in the next loop's row.

Genre: `standard`. Cap 3.

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-08-27 | 3, cold — genre pinned `standard`; packet carried `scripts/local-ci.sh`, `.github/workflows/ci.yml` and `.githooks/pre-push` whole, the measured `amend_field` branches, and the quoted `commits.md` § 4.2 passage on `ants.gate.docsGlob`. Windows / PyInstaller / the GitHub and Analytics APIs / the WordPress archive declared an unrunnable region | 3 | 0 | 2 | n/a | **Five verified, five fixed; two dismissed. First gate on this document** (PRESS-0025). **Three of the five landed on text written minutes earlier**, in the edit being gated. **All three lanes found the same defect, and it is the sharpest:** the new paragraph told a reader to set `ants.gate.docsGlob` because the shared hook otherwise guesses by extension — and the value it prescribed is **byte-identical to that fallback**, so running the command changes no behaviour at all. The true rule is § 4.2's: an *unset* key is the breach, because a shared hook cannot know what a given pipeline reads and must be told. Rewritten to say the key records a decision rather than changing one, and that a narrowing reverts on any clone where it is unset. **All three lanes also found `core.hooksPath` undocumented** — the key that makes any hook fire, equally machine-local, in a section that had just flagged two other keys as lost on a clone. Without it git looks in `.git/hooks`, nothing runs, and nothing says so; `.githooks/pre-push` warns and exits 0 rather than blocking. The guarantee "a failing tree cannot leave this machine" is now qualified to a machine where the keys are set. **And all three found the state block breaching the only rule its own section states** — "keep them to three lines" governing a five-line block, with no way to tell whether the unit is lines, sentences or facts; a session updating it would either delete true content or breach knowingly. Restated as three facts. **Two lanes found `ruff` missing from the dependency sentence**, which claimed no install step beyond `keyring` and `pytest` while the gate runs `ruff check` — so a machine provisioned from that paragraph fails the gate at lint, reported as a lint failure rather than a missing tool. **One lane found the leak-sweep rule false**: it says the only expected hits are the pattern lines in `CLAUDE.md`, and `scripts/local-ci.sh` now carries the same patterns, so a hand-run sweep returns hits the rule calls a leak. **One claim was asserted and is now measured:** the `Layman:` section said a body declaration cannot be turned back into a column. Exercised — `amend_body` deleting the declaration is refused with `render_gate_unmet`, because the bullet would be left carrying none. **Dismissed:** `INV-5` naming two different invariants in this file (a lane judged it inert itself, and nothing built changes), and `docs/standards/` holding a README while § Overrides says "if that directory is empty" — that README states the same convention, so a conformer reading either lands in the same place. |
| 2 | 2026-08-27 | 3, cold — identical brief, packet rebuilt whole from disk and extended with `pyproject.toml`, the § 4.2 quotation, and the two claims loop 1's lanes raised as open questions (the `render_gate_unmet` measurement, and what `docs/standards/` holds) | 2 | 4 | 0 | n/a | **Six verified, six fixed; none dismissed. Half landed on loop 1's own fixes.** **All three lanes found the same three.** The gate guarantee, which loop 1 had qualified to "a machine where the keys below are set", is still not sufficient — `.githooks/pre-push` only delegates, and where the machine-wide hook is absent it prints and exits 0, which the paragraph two below said in loop 1's own words. A conformer setting both keys on a fresh box would treat the push as gated. Now conditioned on the hook being present, with discovery attributed to it rather than to the delegator. **Loop 1's other fix moved a goalpost without moving the block**: "three lines" became "three facts" over a block carrying five, so the cap still meant nothing. The block is trimmed to three; what is queued, blocked or newly filed is read off the roadmap, which is what the rule under it already says. **And the override test can never return its own answer** — "if that directory is empty" against a `docs/standards/` that holds its README index and always will. **Loop 1 dismissed this one as immaterial and was wrong**: it judged that a conformer would open the README and land in the same place, where all three lanes this loop named the sharper case — a session runs `ls`, finds a file, and reports an override that does not exist. Restated against override files rather than emptiness. **Two lanes found the dependency sentence reads as a cap** — "one runtime dependency" against a § Stack that names `Pillow`, so a builder implementing photographs cannot tell whether adding it breaches a rule. Marked as present state. **One lane found the documented commit-message sweep narrower than the gate's**, three identifier fragments against the script's five: a hand-run sweep before a push would pass an analytics id or a LAN address written into a subject line. Widened to match, and the widened form runs clean. **One orchestrator finding, from a lane's open question:** the new paragraph called all three config keys ones that "make the gate work" while saying two lines later that one of them alters no behaviour. Reworded by deletion. |
| 3 | 2026-08-27 | 3, cold — identical brief, packet rebuilt whole from disk; the roadmap ids and the post-edit gate run added as verified facts | 2 | 1 | 1 | n/a | **Four verified, four fixed; one filed. Cap reached (3 for a standard); the tail is empty of unfixed findings and the run exits.** **Half landed on loop 2's own fixes, and the same paragraph has now been repaired in all three loops** — which is the honest signal about this run rather than about the document. **All three lanes found it:** loop 2 conditioned the gate guarantee on "the keys below", which is both of them, while the paragraph nine lines down says `ants.gate.docsGlob` alters no behaviour and a later line presumes gating still runs unset. Only `core.hooksPath` and the machine-wide hook decide whether anything fires; the glob decides which checks run. Stated that way. **Two lanes found the defect this loop existed for**, and it is the sharpest of the three loops: the documented history sweep is `git grep -l`, which prints revision and path and **no matched line** — so the triage rule two paragraphs above it, "the only expected hits are the pattern lines themselves", cannot be applied to its output at all. A genuine historical leak inside `CLAUDE.md` or the gate script produces the same row as the self-reference, and the conformer dismisses it. Confirmed by running both forms: `-l` returns rows for those two files, the script's `-n` plus fixed-string filter returns nothing. Fixed to the script's form, and all three documented sweeps re-run clean as written. **One lane found PyInstaller listed as a runtime dependency** by loop 2's own fix — it is a build-time packager, imported nowhere in `src/`, so a conformer landing packaging would add it to `dependencies` and make every install pull it. **One lane found nothing said whether the hand sweep is still owed once the hook is live**, so a conformer either runs three redundant commands every push or skips them on the ungated machine where they matter most. **Filed, not fixed — code side, out of a docs gate's scope:** PRESS-0032, the gate script's history pass runs a narrower pattern than its other two, so a historical leak of the analytics id or a LAN address is never surfaced. Confirmed by execution. It may be a deliberate cost trade; nothing records that, which is the defect. **A packet defect, caught by a lane and worth recording:** fact 13 said the documented sweeps "returned no hits", when a raw sweep must hit the pattern lines the document calls expected. It meant no *unexpected* hits. **Routing.** Size is not the problem — 287 lines, well inside the budget range, no split indicated. The oscillation is concentrated in the gate-and-keys paragraph this run introduced; every other finding each loop was pre-existing and real. The review of this document as it stands ends here, and the bar lapses with the next authoring edit that changes direction. |
