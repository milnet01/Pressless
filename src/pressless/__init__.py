"""Pressless — the app a writer runs to manage and publish their own site.

__version__ is the one version the program knows, since pyproject.toml is not
in the bundle. It must equal that file's `version` (PRESS-0023 INV-17), and the
Updater compares every release against it.
"""

__version__ = "0.1.2"
