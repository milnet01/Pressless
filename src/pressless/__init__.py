"""Pressless — the app a writer runs to manage and publish their own site.

This file is deliberately empty. It exists so the package resolves once
`marks.py` (PRESS-0004) and its siblings land under this directory; it
carries no code of its own.
"""
